on: [push]

jobs:
  push_config:
    strategy:
      matrix:
        appName: [foo, bar]
    runs-on: ubuntu-latest
    name: Push config
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Push config
        id: config
        uses: ./
        with:
          app-name: test
        env:
          ENV_BAZ: true
          ENV_BAR: false
          ENV_NUM: 1
          ENV_STRING: "OK"
